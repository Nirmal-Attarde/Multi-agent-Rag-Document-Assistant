# Multi-agent-Rag-Document-Assistant


<!-- 
git add .
git commit -m "Update README"
git push

cd "/Users/nirmal/Documents/Perficient/Rag Project"
git status

if we add any new document in Knowlendge base, just rerun the ingest file:
python backend/app/ingest.py

env activate(for mac): source venv/bin/activate

RUN Backend: int (venv)
uvicorn backend.app.main:app --reload --port 8000;  

Go into frontend directory (it needs package.json) then RUN Frontend: This run on localhost 3000 
npm run dev

-->